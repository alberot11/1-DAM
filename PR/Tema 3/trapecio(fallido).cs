//Alberto Girón Serna

using System;
class trapecio
{
    static void Main ()
    {
        long ancho, alto, c, e;
        char caracter;
        Console.Write("introduzca el ancho deseado: ");
        ancho =Convert.ToInt64(Console.ReadLine());
        Console.Write("Introduzca la altura deseada: ");
        alto =Convert.ToInt64(Console.ReadLine());
        Console.Write("Introduzca el caracter: ");
        
        for(int fila= 1; fila<=altura; fila++)
            for(i=0; i<=altura-fila; i++ )
    }
}
